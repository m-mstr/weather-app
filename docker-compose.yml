services:
  db:
    image: postgres:18.3
    container_name: weather_pg
    environment:
      POSTGRES_USER: matt
      POSTGRES_PASSWORD: password
      POSTGRES_DB: weather
    volumes:
      - pg_data:/var/lib/postgresql
    ports:
      - "5431:5432"

volumes:
  pg_data:

